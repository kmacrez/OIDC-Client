@{
    ViewData["Title"] = "Dashboard";
}
<h1>@ViewData["Title"]</h1>

<h3>Hi @ViewData["Username"]</h3>

<p>This is a secure dashboard</p>
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dt>@claim.Value</dt>
    }
</dl>
